import { InferInputDirective } from './infer-input.directive';

describe('InferInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InferInputDirective();
    expect(directive).toBeTruthy();
  });
});
