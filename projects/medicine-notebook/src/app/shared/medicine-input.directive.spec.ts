import { MedicineInputDirective } from './medicine-input.directive';

describe('MedicineInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicineInputDirective();
    expect(directive).toBeTruthy();
  });
});
