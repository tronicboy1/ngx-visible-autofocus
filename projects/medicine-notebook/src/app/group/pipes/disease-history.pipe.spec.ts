import { DiseaseHistoryPipe } from './disease-history.pipe';

describe('DiseaseHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new DiseaseHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
