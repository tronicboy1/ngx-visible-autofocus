import { DoseAdministrationFactory } from './dose-administration-factory';

describe('DoseAdministrationFactory', () => {
  it('should create an instance', () => {
    expect(new DoseAdministrationFactory()).toBeTruthy();
  });
});
