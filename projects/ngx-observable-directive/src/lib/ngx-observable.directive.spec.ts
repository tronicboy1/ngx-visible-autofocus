import { NgxObservableDirective } from './ngx-observable.directive';

describe('NgxObservableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxObservableDirective();
    expect(directive).toBeTruthy();
  });
});
